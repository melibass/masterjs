//CICLO FOR

/*
for (variable de control; condicion; modificacion de variable de control) {
    // cada vez que se cumpla la condicion, ejecuto el bloque de codigo.
    codigo a ejecutar:

}

*/

for (let i = 1; i <= 50; i++) {
    console.log(`El valor de i es: ${i}`)
}

for (let i = 1; i <= 10; i++) {
    let resultado = 12*i;
    console.log(`12 * ${i} = ${resultado}`)
}